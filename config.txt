# Синтаксис следующий:
# это комментарий;
# ":" на конце строки определяет строку как категорию параметров (вкладывает словарь внутрь основного); вложение категорий сделаю, если понадобится
# " = " (именно с пробелами) определяет строку как запись значения параметра, а предшествующий и следующий текст — как название и значение соответственно;
# "emoji_mode" в названии переменной рассматривается отдельно: если за ним следует аттрибут EmojiModes, то в словарь записывается непосредственно аттрибут вместо строки.
# Комментарии, не начинающиеся в начале строки, считаются ересью.

parameters:
# Опции: terra_bot, placeholder, none.
emoji_mode = EmojiModes.terra_bot
offtop_deletion_countdown = 5
override_config = False

roles:
activist_role_id = 1283712029198254090
administrator_role_id = 1273601700728668202
master_role_id = 1302372065374113802
moderator_role_id = 1289906689100742717

channels:
channel_log_delete_id = 946026056849170432
channel_log_hits_id = 1304853335819419778
forum_tasks_id = 1304164705316896841
